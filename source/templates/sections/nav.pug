.nav
    - var items = ['Домой', 'О нас', 'Наши достижения', 'Наши работы' ]
    each item in items
        if item === 'Домой'
            a.nav__link.nav__link--active(href="#")= item
        else
            a.nav__link(href="#")= item